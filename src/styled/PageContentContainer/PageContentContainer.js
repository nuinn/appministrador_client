import styled from 'styled-components';

const PageContentContainer = styled.div`
  margin-bottom: 60px; // equal to or greater than the height of your footer
`;

export default PageContentContainer;
